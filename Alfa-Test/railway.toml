[build]
builder = "NIXPACKS"
buildCommand = "pip install -r Alfa-Test/requirements.txt"

[deploy]
startCommand = "cd Alfa-Test && python -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
